# 全栈天梯 · 学习档案

> 导师带练专用档案：进度 / 课时记录 / 踩坑 / 待巩固点。每次开课先回顾本文件，导师负责维护。

## 基本信息

- 学员背景：前端工程师（Vue3 / TS / ant-design-vue / vxe-table），后端从零起步
- 学习路径：《后端学习路径图谱》（Java + Spring Boot + MyBatis + Redis + Kafka）
- 最终项目：全栈天梯（fullstack-quiz-prd/，PRD v0.4），里程碑 M0–M7
- 练习方式：你写我盯（学员敲码，导师 review + 提问验收）
- 约定：不看外部书籍/博客，所有知识点由导师消化后用「前端对照法」讲解
- 代码目录：`fullstack-ladder/backend`（后端）/ `fullstack-ladder/frontend`（前端，学员自理，联调前就绪即可）

## 环境实测（2026-09-18）

| 组件 | 状态 | 说明 |
|---|---|---|
| JDK 26.0.2.1 | ✅ | **本机唯一真实安装的 JDK**；用户终端 `java` = 26，JAVA_HOME = 26（Maven 用它）；**项目目标版本定为 26** |
| Temurin 21.0.10 | ⚠️ | TRAE 沙箱 shell 自带的 JRE（导师侧终端的 `java` 解析到这里），勿与用户环境混淆 |
| Maven 3.9.16 | ✅ | PATH 可用 |
| VSCode 1.30.1 | ✅ | 主 IDE（替代 IDEA） |
| Apifox / DBeaver / Another Redis Desktop Manager | ✅ | 客户端工具齐，后续都复用 |
| MySQL Server 8 | ✅ | 装在 WSL Ubuntu，手动启动（随用随起）；Windows 侧 DBeaver 已连通 |
| Redis Server | ✅ | 装在 WSL Ubuntu，手动启动；Another Redis Desktop Manager 已连通 |
| WSL2 Ubuntu | ✅ | 用作 MySQL/Redis 服务端环境；开发会话开场：`wsl -u root systemctl start mysql redis-server` |

## 课程规划（对齐 PRD 里程碑）

| 课次 | 里程碑 | 主题 |
|---|---|---|
| 第 1 课 | M0 | Spring Boot 工程生成 + 第一个接口 /api/health |
| 第 2 课 | M0 | WSL 装 MySQL + Redis，客户端连通验证（含 Linux 基础命令） |
| 第 3 课 | M0 收尾 | 智能体 mock 层 + 依赖注入概念；git init |
| 第 4 课起 | M1 | 表设计 + MyBatis + CRUD 分层 + 统一返回体 |
| 后续 | M2–M7 | 出题智能体对接 / 判分与事务 / Redis 排行榜 / 错题本 / 联调封版 / 鉴权·Kafka·部署（选做） |

> **M0 ✅ 全部完成**（2026-09-18，第 1–3 课）· 当前：M1 进行中（第 4 课）

## 课时记录

### 第 1 课 · M0：起一个 Spring Boot 工程（2026-09-18，✅ 已完成）

- 目标：亲手生成并跑通第一个 Spring Boot 工程，写出第一个接口
- 概念点：Maven↔pnpm、pom.xml↔package.json、~/.m2↔node_modules、内嵌 Tomcat↔dev server、注解声明式路由
- 结果：一次跑通，无卡点。验收三题：parent 继承 ✓、.m2/GAV ✓、@RestController↔vue-router 半对（已纠正：path→method 的映射是 @GetMapping 的职责）；代码 review 已完成（up→UP 契约、Boot 4 starter 改名 web→webmvc、端口契约定为 8081）；工程已由 F:\exercise\java_study\backend 迁移至 `fullstack-ladder/backend`，后续在此开发

### 第 2 课 · M0：WSL 里装 MySQL + Redis 服务端（2026-09-18，✅ 已完成）

- 目标：服务端就绪，Windows 侧 DBeaver / Redis Desktop Manager 连通验证
- 概念点：apt↔pnpm、systemctl↔服务管理（start/enable/stop/status ↔ Windows"启动类型+启动按钮"）、sudo、守护进程、端口、WSL 生命周期（关终端后虚拟机约 8 秒停止）、人生第一批 SQL
- 任务：apt 安装 mysql-server / redis-server → **手动启动（学员明确要求随用随起，不设开机自启）** → redis-cli ping → 建 ladder 库和账号 → DBeaver/RDM 连通；每次开发会话开场咒语：`wsl -u root systemctl start mysql redis-server`
- 结果：✅ 完成。MySQL/Redis 服务端就绪（手动启动模式，随用随起）、DBeaver/RDM 连通；验收三题全过；踩坑 Public Key Retrieval（见踩坑记录）

### 第 3 课 · M0 收尾：依赖注入 + 智能体 mock 层 + git 立库（2026-09-18，✅ 已完成）

- 目标：跑通 GET /api/questions/daily（Mock 出题），工程 git 立库，M0 全部完成
- 概念点：依赖注入（@Component↔provide、构造器注入↔props 显式传入）、面向接口编程（契约先行）、一个文件一个 public 类、Bean↔容器单例
- 任务：git init + 首次 commit → agent 包（QuestionAgentClient 接口 + DailyQuestion record + MockQuestionAgentClient @Component）→ QuestionController 构造器注入 → 跑通 JSON → 删 @Component 制造事故再修复
- 结果：✅ 完成。跑通 /api/questions/daily、git 立库、删 @Component 事故实验已做；验收：题 1 答偏（误答"改接口文件"——正确是契约不动、新增实现类切换，已重点纠偏，M2 接真实智能体时复考）、题 2 直觉对（点名具体类可行；正统是启动报 NoUniqueBeanDefinitionException + @Primary/@Qualifier/@Profile 消歧，M2 实战）、题 3 因果反了（单例≠线程安全，无状态才安全；已引入"每请求一线程"并发模型）

### 第 4 课 · M1-1：天梯第一批表设计 + 建表（2026-09-18，已布置）

- 目标：ladder 库建 users / questions / answer_records 三表 + 测试数据，建立关系模型思维
- 概念点：关系思维 vs JSON 思维、DDL/DML、自增代理主键、逻辑外键 vs 物理外键、JSON 列 vs 拆表的取舍、索引初步、审计字段惯例
- 任务：DBeaver 敲 DDL 建三表 → DESC 验证 → INSERT 测试数据（Mock 三题入库）→ 思考题（作答记录为何存 id 不存题目内容）
- 状态：进行中，待学员动手

## 踩坑记录

- 2026-09-18 环境误判勘误：导师在 TRAE 沙箱 shell 里 `java -version` 得到 Temurin 21，误判为用户默认 JDK；实为 TRAE 自带 JRE。用户本机终端 `java` 与 JAVA_HOME 均为 JDK 26，项目目标版本定为 26（Spring Boot 4.1.1+ 官方兼容到 Java 26）。
- 2026-09-18 DBeaver 连 MySQL 报 `Public Key Retrieval is not allowed`：MySQL 8 默认认证插件 caching_sha2_password + JDBC 驱动默认拒绝在明文信道取服务器公钥（防中间人）。解法：连接的 Driver properties 里 `allowPublicKeyRetrieval=true`。注意 M1 的 JDBC URL 同样要带此参数。

## 待巩固点

- @RestController（类是一组 HTTP 接口 + 返回值序列化 JSON）与 @GetMapping（path→method 路由映射）的职责边界——第 1 课验收半对已纠正，后续观察
- Spring Boot 4 starter 改名（spring-boot-starter-web → webmvc 等）：查老资料时注意新旧名字映射，勿混用
- 面向接口的切换本质（契约不动/实现可换/依赖倒置）——题 1 答偏已纠，M2 接真实智能体时复考
- 单例 Bean 必须无状态 + "每请求一线程"并发模型——题 3 因果已纠，M1 写 Service 时持续观察
